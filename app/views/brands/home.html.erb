

<div class="background">

        <div class="overlay" style=" position: absolute;width: 100%; height: 100vh;text-align: left;">

            <div class="col-md-12">
                <nav class="navbar" style="margin-top: 1%;">
                    <div class="navbar-brand">
                        <h1 class="log-1">Fits<span>Me</span></h1>
                    </div>
                    <ul class="nav" style="margin-right: 1%">
                        <%= link_to 'Size Quiz', new_customer_datum_path, class: "nav-link", style:'color: white'%>
                        <li class="nav-item">
                            <%# <a href="#" class="nav-link">Sign in</a> %>
                            <% if user_signed_in?  %>
                              <%= link_to 'Log Out', edit_user_registration_path, class: "nav-link"%>
                            <% else %>
                              <%= link_to 'Sign in', new_user_session_path, class: "nav-link" %>
                            <% end %>
                        </li>
                    </ul>
                </nav>
            </div>

            <div class="info">
                <h2 class="text-left">Seamless Sizing, Brand Agnostic .</h2>
                <p class="text-left mb-5">
                    The World's top apparel companies use FitsMe to connect <br> clothing to customers, boost conversation and slash returns.
                </p>

                <a href="<%= products_path %>" class="button-main py-2 px-5 ">Brand</a>
                <a href="customer_data/new" class="button-main py-2 px-4 " style="margin-left:15px">Fits Quiz</a>
            </div>

        </div>

    </div>
